// native_test_anchor.cpp
// Placeholder translation unit included by the [env:native] build_src_filter.
// Keeps the native env from inheriting Arduino-dependent sources while still
// producing a valid (empty) compilation unit.
// PowerManager stubs live in test/test_native/power_mgr_stubs.cpp so that
// PlatformIO's test runner always links them automatically.
